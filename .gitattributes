# Normalize text files to LF in the repository; Git checks out native on Windows.
* text=auto eol=lf

# Treat binary assets as binary (no line-ending conversion).
*.png binary
*.ico binary
*.ttf binary
*.woff binary
*.woff2 binary
*.exe binary
